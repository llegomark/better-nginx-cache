{
    "require-dev": {
        "php-stubs/wordpress-stubs": "^6.9",
        "phpunit/phpunit": "^12.5.6",
        "brain/monkey": "^2.6.2",
        "mockery/mockery": "^1.6.12"
    },
    "autoload-dev": {
        "psr-4": {
            "BetterNginxCache\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "test:coverage": "phpunit --coverage-html coverage"
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    }
}